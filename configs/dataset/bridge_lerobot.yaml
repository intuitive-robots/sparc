# @package _global_
# bridge_lerobot (LeRobot format)

dataset:
  name: "bridge_lerobot"
  type: "bridge_lerobot"
  root: ???  # required: dataset.root=/path/to/dataset
  fps: 5.0
  fov_x: 55.5
  image_width: 640
  image_height: 480
  task_obj_dict_path: "${dataset.root}/bridge_lerobot_task_obj.pkl"

annotator:
  gpu_ids: [0, 1, 2, 3]
  n_processes_per_gpu: 4

debug:
  n_trajectories: 500   # remove to annotate the whole dataset
