# @package _global_
# agibot_world (LeRobot format)

dataset:
  name: "agibot_world"
  type: "agibot_world_multi"
  root: ???  # required: dataset.root=/path/to/dataset
  fps: 30.0
  camera_fx: 322.3
  camera_fy: 429.2
  camera_cx: 323.5
  camera_cy: 242.5
  image_width: 640
  image_height: 480
  task_obj_dict_path: "${dataset.root}/agibot_world_task_obj.pkl"
  max_per_task: 50

annotator:
  gpu_ids: [0]
  n_processes_per_gpu: 4
