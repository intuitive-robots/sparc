# @package _global_
# oxe_lerobot (LeRobot format)

dataset:
  name: "oxe_lerobot"
  type: "oxe_lerobot"
  root: ???  # required: dataset.root=/path/to/dataset
  fps: 10.0
  image_width: null
  image_height: null
  task_obj_dict_path: "${dataset.root}/oxe_lerobot_task_obj.pkl"
  loader_kwargs:
    config_path: "configs/dataset/oxe_config/oxe_lerobot.yaml"
    subdatasets: null

annotator:
  gpu_ids: [0]
  n_processes_per_gpu: 4

debug:
  n_trajectories: 500   # remove to annotate the whole dataset
